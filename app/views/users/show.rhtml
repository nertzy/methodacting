<h1><%=h @user.name %> (User)</h1>

<h2>Actors created by <%=h @user.name %></h2>
<ul>
  <% @user.actors.each do |actor| -%>
    <li><%= link_to h(actor.name), actor_path(actor) %></li>
  <% end -%>
</ul>
<p><%= link_to 'Add a new actor', new_actor_path(:creator_id => @user) %></p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>